# ---- build artifacts (in-process demo) ----
backend
demo
mitigation
probe
*.log
*.o

# ---- Go proxy/client binaries (networked + ecosystem PoCs) ----
vuln_proxy
strict_proxy
attacker
victim

# ---- subagent scratch / language installs (not deliverables) ----
*_work/
node_modules/
.bun/
bun.lockb
package-lock.json

# ---- ecosystem PoC dirs (uwebsockets-js/, hyper-express/, bun/): verified,
#      finalized, source-only tracked. node_modules, Go binaries, *.log, *.pid and
#      package-lock.json stay ignored by the rules above plus each dir's .gitignore.
